name: Greeting from Marky

on:

push:
     branches:
     - main
 jobs:
     build:

    runs-on: ubuntu-latest

    steps:

      - name: checkout repo
        uses: actions/checkout@v2

          - name: Print message

        run: |
             echo "hello world, this is a samplen CI/CD"
             
          
